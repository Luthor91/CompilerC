#include "..\header\server_config.h"

struct server_config server_config = {
    .port = SERVER_PORT,
    .max_connections = MAX_CONNECTIONS,
    // Initialisez les autres paramètres de configuration si nécessaire
};
